import { IterNumericInterfacePipe } from './iter-numeric-interface.pipe';

describe('IterNumericInterfacePipe', () => {
  it('create an instance', () => {
    const pipe = new IterNumericInterfacePipe();
    expect(pipe).toBeTruthy();
  });
});
