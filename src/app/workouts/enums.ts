export enum WeightUnit {
  Pounds = 1,
  Kilograms = 2,
}

export enum Focus {
  Strength = 1,
  Speed = 2,
  Endurance = 3,
  Mobility = 4,
}

export enum Measure {
  Weight = 1,
  Reps = 2,
  Distance = 3,
  Time = 4,
}

export enum DistanceUnit {
  Miles = 1,
  Kilometers = 2,
  Meters = 3,
}

export enum Benchmark {
  Weight = 1,
  Reps = 2,
  Rounds = 3,
  RoundsAndReps = 4,
  Distance = 5,
  Time = 6,
}
